export const TOTAL_BUGS = 10;
export const HOVER_MS = 1000;
export const VISIBLE_LINES = 20; // default / SSR fallback
export const LINE_HEIGHT = 22; // px
export const FILE_HEADER_HEIGHT = 34; // px — must match .fileHeader height in CodeViewer CSS
